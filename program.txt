main() {
    while ((funca(1, 2, funcb())-1+3) * (12/3) - 5) {
        while (3 + variable) {
            a = variable + 3;
        }
        a = a + a;
        var z, y , x;
        b + aba();
        if(if) { dosomething(); }
    }
}
var x, x;
